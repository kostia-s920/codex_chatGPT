version: "3.9"

services:
  threads-agent:
    build: .
    container_name: threads-telegram-agent
    env_file:
      - .env
    environment:
      - DB_PATH=/app/data/threads-agent.db
    volumes:
      - threads_agent_data:/app/data
    restart: unless-stopped

volumes:
  threads_agent_data:
